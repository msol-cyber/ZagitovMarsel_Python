n =  int(input("Введите любое число: "))
print(n + int(str(n) + str(n)) + int(str(n) + str(n) + str(n)))

#Сначала переводим число в строку через оператор str, потом склеивам нужное количество переведенных переменных и заключаем их все в скобки после чего переводм их в число и делаем сложение
