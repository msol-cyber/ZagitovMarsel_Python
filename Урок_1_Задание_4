x = int(input("Введите положительное число: "))
mx = 0 # делаем точку отчета от 0
while x > 0: # X должен быть больше 0 в цикле
    c = x % 10 # Создаем переменную которая говорит нам об остаточном деление на 10 так как в количество цифр варируется от 1 до 9
    if c >= mx: # Создаем условие при котором полученная переменная сравнивает себя от стартовой точки переменной mx
        mx = c # Как только программа сравнила, она присваивает переменной mx значение переменной с
    x = x // 10 # дальше убираем из введенного числа единицы или проще говоря отрубаем ей хвост и так все прока не дойдем до первой цифры
print(mx)
