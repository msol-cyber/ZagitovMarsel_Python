#Запросите у пользователя значения выручки и издержек фирмы. Определите,  с каким финансовым результатом работает фирма. Например, прибыль - выручка больше издержек, или убыток -- издержки больше выручик
x = int(input("Введите сумму выручки: ")) #ВЫРУЧКА
y = int(input("Введите сумму издержек: ")) #РАСХОДЫ
z = x - y #Прибыль
if x > y:
	print("Фирма работает в плюс, чистая прибыль составляет: ", z)
	b = (x - y) / y * 100
	print("Рентабельность реализованной продукции составляет: ", b)
	a = z / x * 100
	print("Рентабельность выручки: ", a)
	pepople = int(input("Введите количество сотрудников: "))
	pepople_z = z / pepople
	print("Прибыль с учетом на одного сотрудника составляет:", pepople)
elif x < y:
	print("Фирма работает в убыток, расхожы комании превышают выручку на:", z)
	b = (x - y) / y * 100
	print("Рентабельность реализованной продукции составляет: ", b)
