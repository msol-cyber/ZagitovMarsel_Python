a = int(input("Введите растояние в первый день: "))
b = float(input("Введите растояние на сегодняшний день: "))
an = a
x = 1
z = 1
while x < 100 and a > 0:
    print(x, "-й День: ", an)
    x = x + 1
    an = an + ((an * 10) / 100)
    if z == (an // b):
        break
print("Сегодня: ", x, "День")
